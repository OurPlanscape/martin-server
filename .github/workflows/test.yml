name: Lint Martin Configuration

on:
  push:
    branches-ignore:
      - main

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lint
        uses: frenck/action-yamllint@v1
        with:
          path: martin.yaml
